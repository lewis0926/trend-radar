Trend Radar is a real-time dashboard identifying which industries are trending based on past price action.

## Project
Track a list of ETFs with exposure to all major market sectors — tech, healthcare, finance, energy, consumer discretionary, and more.

## Tech Stack
- **Backend**: Python 3.10+, FastAPI + uvicorn, venv for dependency management
- **Frontend**: Vite + React + TypeScript (`frontend/`), Bun for package management
- **Config**: Pkl — generate `config.json` from a Pkl file and parse it in the app

## Config Structure
- `config/schema/Config.pkl` — typed schema. Defines `etfs` (list) and `notableMoverThreshold` (float).
- `config/<env>.pkl` — environment-specific override (e.g. `dev.pkl`).
- `config.json` — generated by running `pkl eval "config/<env>.pkl" -f json -o "config.json"`. Gitignored.

Market indices and commodities are **not** in config — they are hardcoded in `backend/src/api.py` as `_INDICES` and `_COMMODITIES`.

## Running
```bash
# Backend API (from backend/)
cd backend && source ../venv/bin/activate
uvicorn src.api:app --reload

# Frontend (from frontend/)
cd frontend && bun run dev
```

## Adding a New ETF
1. Add the typed entry to `backend/config/schema/Config.pkl`
2. Override or extend in `backend/config/<env>.pkl` if needed
3. Regenerate: `cd backend && pkl eval "config/dev.pkl" -f json -o "config.json"`

## Deployment
- `backend/Dockerfile` — runs uvicorn; `config.json` must be mounted at runtime (`-v /path/to/config.json:/app/config.json`)
- `frontend/Dockerfile` — multi-stage: Bun builds static assets, nginx serves them and proxies `/api/` to `http://backend:8000`
- `.github/workflows/build.yml` — builds both images on every push; pushes to GHCR with `<sha>` tag and `latest` (main branch only); image names follow `trend-radar-{backend|frontend}-<branch>`

## Coding Standards
- `config.json` is the single config file. All configuration goes in env Pkl files and flows into `config.json`. No `.env` files, no environment variables.
- All Python functions must have explicit type annotations on parameters, return values, and newly defined variables.

## Frontend Coding Standards

**Component structure**
- Each logical UI section gets its own component file in `frontend/src/components/`.
- Components with non-trivial styles get a co-located `.css` file (e.g. `Header.tsx` + `Header.css`).
- `App.tsx` is a thin orchestrator: data fetching + layout only, no styling.

**CSS over inline styles**
- All styling goes in CSS files — no inline `style={{}}` props except for values that are purely data-driven at runtime (e.g. a bar width from a percentage, a color derived from a positive/negative value).
- Design tokens (colors, fonts) live as CSS custom properties in `index.css` under `:root`. Reference them as `var(--orange)` etc. — never hardcode hex values in component CSS or JS.
- Common utility classes (`.mono`, `.tabular`, `.positive`, `.negative`) are defined in `index.css` and composed in JSX `className`.
- Shared structural patterns (`.section-title`, `.page`) are defined in `index.css`; component-specific styles go in the component's own CSS file.
- Mobile layout is handled with `@media (max-width: 640px)` blocks in each component's CSS file.
